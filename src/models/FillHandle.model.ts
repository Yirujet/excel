namespace Excel {
  export namespace FillHandle {
    export interface FillHandleInstance {
      width: number | null;
      height: number | null;
      position: Excel.Position;
    }
  }
}
